## Epicurious data scraper
- Python Version: 3.6.2
- Create virtualenv with `virtualenv venv -p python3`
- Pulls data from Epicurious links from json `recipes.json`
- Can configure pip with `pip install -r requirements.txt`